diff --git b/bot-rs/src/schema.rs a/bot-rs/src/schema.rs
index 703f513..9285e19 100644
--- b/bot-rs/src/schema.rs
+++ a/bot-rs/src/schema.rs
@@ -63,12 +63,50 @@ table! {
         id -> Int4,
         discordid -> Int8,
         name -> Text,
     }
 }
 
+table! {
+    deck_records (id) {
+        id -> Int4,
+        match_wins -> Int8,
+        match_losses -> Int8,
+        game_wins -> Int8,
+        game_losses -> Int8,
+    }
+}
+
+table! {
+    leaderboard (id) {
+        id -> Int4,
+        name -> Text,
+        league -> Int4,
+        wins -> Int8,
+        complete_runs -> Int8,
+    }
+}
+
+table! {
+    cards (id) {
+        id -> Int8,
+        name -> Text,
+        number -> Text,
+        setcode -> Text,
+        isarena -> Int8,
+        scryfalloracleid -> Uuid,
+        manacost -> Nullable<Text>,
+        types -> Text,
+        convertedmanacost -> Float8,
+        side -> Nullable<Text>,
+    }
+}
+
+joinable!(deck_records -> decks (id));
+joinable!(leaderboard -> users (id));
+joinable!(leaderboard -> leagues (league));
 joinable!(deck_contents -> decks (deck));
 joinable!(deck_view_tokens -> decks (deck));
 joinable!(decks -> leagues (league));
 joinable!(decks -> users (owner));
 joinable!(disputes -> matches (matchid));
 joinable!(disputes -> users (disputer));
@@ -78,7 +115,10 @@ allow_tables_to_appear_in_same_query!(
     deck_view_tokens,
     decks,
     disputes,
     leagues,
     matches,
     users,
+    cards,
+    deck_records,
+    leaderboard,
 );
